{
  "routes": [
    {
      "method": "GET",
      "path": "/products",
      "handler": "products.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/products",
      "handler": "products.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/products",
      "handler": "products.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
