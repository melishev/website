{
  "name": "melishev",
  "version": "3.0.0",
  "private": true,
  "scripts": {
    "setup": "yarn && cd back && yarn && cd .. && cd front && yarn",
    "dev:back": "yarn --cwd back dev",
    "dev:front": "wait-on http://localhost:1337/admin && yarn --cwd front dev",
    "dev": "cross-env FORCE_COLOR=1 npm-run-all -l -p dev:*"
  },
  "devDependencies": {
    "cross-env": "7.0.3",
    "npm-run-all": "4.1.5",
    "wait-on": "5.2.1"
  }
}