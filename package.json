{
  "name": "melishev",
  "version": "3.0.0",
  "private": true,
  "scripts": {
    "setup": "yarn && cd api && yarn && cd .. && cd client && yarn",
    "dev:back": "yarn --cwd api dev",
    "dev:front": "wait-on http://localhost:1337/admin && yarn --cwd client dev",
    "dev": "cross-env FORCE_COLOR=1 npm-run-all -l -p dev:*"
  },
  "devDependencies": {
    "cross-env": "7.0.3",
    "npm-run-all": "4.1.5",
    "wait-on": "5.2.1"
  }
}